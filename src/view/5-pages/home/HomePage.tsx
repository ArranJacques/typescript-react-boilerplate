import React, { PureComponent, ReactNode } from 'react';
import AppTemplate from 'view/4-templates/app/AppTemplate';

export default class HomePage extends PureComponent {
    public render(): ReactNode {
        return <AppTemplate />;
    }
}
